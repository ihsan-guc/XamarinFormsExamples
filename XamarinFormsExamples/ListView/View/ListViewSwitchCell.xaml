<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsExamples.ListView.View.ListViewSwitchCell">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Model}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <SwitchCell Text="{Binding DepartmentName}" OnColor="Aqua" IsEnabled="{Binding IsCheck}"
                                    Height="20"></SwitchCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>