<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsExamples.ListView.View.ListViewEntryCell">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Model}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <EntryCell Text="{Binding DepartmentId}"
                                   Label="{Binding DepartmentName}" LabelColor="Black">
                        </EntryCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>