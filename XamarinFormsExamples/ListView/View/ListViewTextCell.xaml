<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFormsExamples.ListView.View.ListViewTextCell">
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Model}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding DepartmentName}" TextColor="Black"
                                  Detail="{Binding DepartmentId}" DetailColor="Aqua"></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>